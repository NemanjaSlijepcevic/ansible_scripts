---
- name: "Update Automation"
  hosts: automations
  gather_facts: true
  remote_user: "{{ user.name }}"
  become: true
  vars:
    current_host: "automation"
  roles:
    - { role: common, tags: ['common'] }
    - { role: authelia, tags: ['authelia', 'proxy', 'security'] }
    - { role: traefik, tags: ['traefik', 'proxy', 'security'] }
    - { role: crowdsec, tags: ['crowdsec', 'proxy', 'security'] }
    - { role: socket_proxy, tags: ['socket_proxy', 'runners', 'automation'] }
    - { role: claude_runner, tags: ['claude_runner', 'runners', 'automation'] }
    - { role: ansible_runner, tags: ['ansible_runner', 'runners', 'automation'] }
    - { role: kestra, tags: ['kestra', 'automation'] }
    - { role: alloy, tags: ['alloy', 'metrics', 'logs', 'monitoring'] }
