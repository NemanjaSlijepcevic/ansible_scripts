---
user:
  name: "deploy"
  group: "docker"

puid: "1000"
pgid: "1000"

music_drive: "/mnt/music"
download_drive: "/mnt/downloads"

postgres:
  ip: "192.0.2.10"
  port: 5432
  user: "mediauser"
  password: "changeme"

lidarr:
  api_key: "changeme"
  port: "8686"
  host: "Host(`lidarr.example.com`)"
  url: "https://lidarr.example.com"

# Sibling services lidarr connects to (media server + download client).
jellyfin:
  api_key: "changeme"

transmission:
  user: "changeme"
  password: "changeme"
