http:
  routers:
    docker-metrics:
      entryPoints:
        - "https"
      rule: "{{ node.docker.host }}"
      middlewares:
        - chain-basic-auth
      tls: {}
      service: docker-metrics
  services:     
    docker-metrics:
      loadBalancer:
        servers:
          - url: "{{ node.docker.url }}"
        #passHostHeader: true