---
- name: Install Adminer
  community.docker.docker_container:
    name: adminer
    image: adminer
    pull: true
    state: started
    restart_policy: unless-stopped
    networks:
      - name: proxy
    env:
      TZ: "Europe/Belgrade"
    labels:
      traefik.enable: "true"
      traefik.http.routers.adminer.entrypoints: "https"
      traefik.http.routers.adminer.rule: "{{ adminer.host }}"
      traefik.http.routers.adminer.tls: "true"
      # chain-tunnel is unusable on the public server: the IP-updater
      # container only maintains default-whitelist.yml, so tunnel-whitelist
      # never learns the current home IP → 403 for everyone.
      traefik.http.routers.adminer.middlewares: "chain-auth@file"
      traefik.http.services.adminer.loadbalancer.server.port: "{{ adminer.port }}"
