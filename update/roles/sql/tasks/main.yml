---
- name: Deploy MariaDB databases
  ansible.builtin.include_tasks: generate_container.yml
  loop: "{{ db_server }}"
  loop_control:
    loop_var: db

- name: Install adminer
  ansible.builtin.include_tasks: install_adminer.yml
